<!--components/navi/index.wxml-->
<view class="container">
    <image class="icon" src="{{latest ? disLeftIcon : leftIcon}}" catch:tap="goToNextClassic"></image>
    <text class="title">{{title}}</text>
    <image class="icon" src="{{first ? disRightIcon : rightIcon}}" catch:tap="goToPreClassic"></image>
</view>