<!--logs.wxml-->
<view class="container">
    <view class="books-container">
        <v-book wx:for="{{books}}" wx:index="index" wx:key="{{index}}" book="{{item}}" class="book"></v-book>
    </view>
</view>